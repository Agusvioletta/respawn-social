export default function FeedPage() {
  return (
    <div style={{ maxWidth: '600px', margin: '0 auto', padding: '24px 16px' }}>
      <h1 style={{
        fontFamily: 'var(--font-display)',
        fontSize: '18px',
        letterSpacing: '2px',
        color: 'var(--cyan)',
        marginBottom: '24px',
      }}>
        FEED
      </h1>
      <p style={{
        fontFamily: 'var(--font-mono)',
        fontSize: '13px',
        color: 'var(--text-muted)',
      }}>
        // Feed en construcción — Fase 2
      </p>
    </div>
  )
}
